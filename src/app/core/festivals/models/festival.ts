export interface Festival {
  id: string;
  title: string;
  startDate: string;
  endDate: string;
  imageUrl?: string;
  location: string;
  transportArranged: boolean;
  createdAt: string;
  updatedAt: string;
}
